# =============================================================================
# MEJ Robot Framework Tests - Environment Variables
# =============================================================================
# IMPORTANT: Copy this file as .env and fill in the values!
#           NEVER commit .env file to git (it's in .gitignore)
#
# USAGE:
#   1. Local development:  cp .env.example .env && nano .env
#   2. Docker:             docker-compose run --rm rf-tests
#   3. CI/CD:              Values are passed from TeamCity parameters
# =============================================================================

# --- Application URL ---
# URL of the application under test (in CI/CD this is set dynamically)
MEJ_URL=
MEJ_API_URL=

# --- Test Configuration ---
MEJ_HEADLESS=True
MEJ_TIMEOUT=0
MEJ_LOG_TO_CONSOLE=

# --- Credentials - Admin ---
MEJ_ADMIN_EMAIL=
MEJ_ADMIN_PASSWORD=

# --- Credentials - Driver ---
MEJ_DRIVER_EMAIL=
MEJ_DRIVER_PASSWORD=

# --- Credentials - Company ---
MEJ_COMPANY_EMAIL=
MEJ_COMPANY_PASSWORD=

# --- API Key ---
# API key for accessing MEJ API (X-Mej-API-Key header)
MEJ_API_KEY=

# --- Database ---
# In CI/CD, DB_NAME is set dynamically: MEJ-dev-{branch}
MEJ_DB_HOST=
MEJ_DB_PORT=
MEJ_DB_NAME=
MEJ_DB_USER=
MEJ_DB_PASSWORD=
